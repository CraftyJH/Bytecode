# Bytecode — Project Instructions

## Git workflow

- Push all commits directly to `main`. Do not create feature branches or pull requests unless explicitly asked.
- Use `git push -u origin main` after every commit.
